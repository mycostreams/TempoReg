def gen_background(grid_seed: int, time_seed: int):
    """
    Function to generate a synthetic background image for testing purposes. The background will be generated based on the provided grid and time seeds, which will determine the spatial and temporal patterns in the background.

    Args:
        grid_seed (int): Seed for generating spatial patterns in the background.
        time_seed (int): Seed for generating temporal patterns in the background.

    Returns:
        np.ndarray: Generated background image as a numpy array.
    """
    pass